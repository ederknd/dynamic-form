<%= turbo_frame_tag @option do %>
  <%= form_with(model: @option,
        url: question_options_path,
        data: { turbo_frame: 'options' }) do |form| %>
    <%= form.label(:name) %>
    <%= form.text_field(:name) %>

    <%= form.submit %>
  <% end %>
<% end %>
